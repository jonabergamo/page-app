<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Primeiro, defina o shape que funcionará como fundo -->
    <item android:drawable="@drawable/rounded_bg" />

    <!-- Em seguida, adicione a imagem que deseja exibir -->
    <item
        android:drawable="@drawable/add"
        android:gravity="center"
        android:width="40dp"
        android:height="40dp"/>
</layer-list>